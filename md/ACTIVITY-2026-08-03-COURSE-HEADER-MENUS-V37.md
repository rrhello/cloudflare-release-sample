# v37 — Course Header and Dropdown Menus

- Forces the course header app-launcher icon to remain white against the RCC
  charcoal navigation background.
- Restores All and Unread as full-width pill tabs with explicit height, padding,
  spacing, active state, hover state, and focus state.
- Restores dark profile greeting and identity text plus readable secondary email
  text across both native GHL profile-menu mount strategies.
- Restores the logout/action row to a full-width, padded control instead of a
  compressed box.

## QA

On a course landing page, verify the top-right launcher icon, both notification
tabs, notification scrolling, profile greeting, email, account actions, and
logout row at desktop, tablet, and mobile widths. Confirm the same controls on
Portal Home remain unchanged.
